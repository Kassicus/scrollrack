# Companion

This is the companion application that controls the entire Scroll Rack program

It will recieve and store owned cards into a database, log which cards are stored in which bins from the sorter and handle fetching cards for automatic deck construction
